mode = "run-pass"
ignore = true  # function pointer calls not yet implemented

[expect.run]
return = 42

[source]
inline = """
fn sum3(_ a: i32, _ b: i32, _ c: i32): i32 { a + b + c }
fn main(): i32 {
    let f: fn(i32, i32, i32) -> i32 = sum3;
    return f(10, 12, 20);
}
"""
