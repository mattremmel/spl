# `as` casts removed per spec: use .widen()/.truncate()/.try_into() instead
mode = "compile-fail"

[source]
inline = """
fn main(): i32 {
    let x: i64 = 42;
    let y = -x as i32;
    return y + 100;
}
"""
