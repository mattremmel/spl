# `as` casts removed per spec: use .widen()/.truncate()/.try_into() instead
mode = "compile-fail"

[source]
inline = """
fn main(): i32 {
    let a: i64 = 84;
    let b: i64 = 2;
    return (a / b) as i32;
}
"""
